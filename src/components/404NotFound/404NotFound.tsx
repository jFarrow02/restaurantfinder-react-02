import { ReactElement } from "react";

const NotFound = (props: any): ReactElement => {
    return(
        <div className="404NotFound">
            404 Not Found
        </div>
    );
};

export default NotFound;